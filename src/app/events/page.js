import Offer from "@/components/calendar/Offer";
import Wrapped from "@/components/calendar/WrappedEvents";

const page = () => {
  return (
    <div>
      <Offer />
      <Wrapped />
    </div>
  );
};

export default page;
