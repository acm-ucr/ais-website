import Title from "@/components/Title";
import React from "react";

const Home = () => {
  const word = "About AIS";
  return (
    <div>
      <Title title={word} />
    </div>
  );
};

export default Home;
