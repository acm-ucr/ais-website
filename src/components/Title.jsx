import React from "react";

const Title = ({ title }) => {
  return (
    <div className="text-4xl text-ais-blue-100 font-semibold font-nunito">
      {title}
    </div>
  );
};
export default Title;
